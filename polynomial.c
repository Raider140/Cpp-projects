#include <stdio.h>

int main(){

    int x, power, sum;
    printf("What would you like as the base? : ");scanf("%d", &x);
    printf("What would you like as the power? : ");scanf("%d", &power);

    for (int i, i>power, i++){

        
    }

}